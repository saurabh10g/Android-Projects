package com.saurabh.kotlinbasics

/*
    PUBLIC    (default)
    PRIVATE
    PROTECTED
    INTERNAL   = Only visible inside the module.
*/

// OPEN KEYWORD
/* In Kotlin all classes are final as default so, to inherit the classes we need to declare it open
    Unlike kotlin,in java has all classes are available to inherited, we have to make them final.
 */